# ローカル開発・プレビューガイド

自分のパソコンでブログの表示を確認（プレビュー）する方法です。

## 1. 準備
Pythonがインストールされている必要があります。

1. このプロジェクトのフォルダをターミナルで開きます。
2. 必要なライブラリをインストールします。
   ```bash
   pip install -r requirements.txt
   ```

## 2. サイトの構築（ビルド）
以下のコマンドを実行すると、`content/` の内容を元に `dist/` フォルダの中に実際のウェブサイト用ファイルが生成されます。
```bash
python jules.py
```

## 3. プレビューの実行
生成されたサイトをブラウザで見られるようにします。
```bash
python -m http.server -d dist 8000
```
その後、ブラウザで `http://localhost:8000/jp/` を開くと、作成した記事を確認できます。

## ミスを防ぐポイント
- **ファイルを保存してからビルド:** 記事を編集した後、ファイルを「保存（Ctrl+S）」してから `python jules.py` を実行しないと、変更が反映されません。
- **ターミナルの場所:** 常に `jules.py` があるフォルダ（プロジェクトのルート）でコマンドを実行してください。
