# トラブルシューティングガイド (Troubleshooting)

このブログシステムを運用する中で発生する可能性のある問題とその解決策をまとめました。

---

## 1. サイト更新・ビルドの問題

### Q. GitHubにpushしたのにサイトが更新されない
- **原因1: ビルドの失敗**
  - Cloudflare Pagesのダッシュボードで「Deployments」を確認してください。エラー（Failed）が出ている場合は、ログを見て原因（ライブラリ不足など）を特定します。
- **原因2: キャッシュ**
  - ブラウザのキャッシュが効いている場合があります。`Ctrl + F5` (Windows) または `Cmd + Shift + R` (Mac) で強制リロードを試してください。

### Q. ビルド時に `ModuleNotFoundError: No module named 'jinja2'` と出る
- **解決策:** `requirements.txt` に `Jinja2` が含まれているか確認してください。Cloudflareはこれを見て必要なライブラリをインストールします。

---

## 2. 表示・デザインの問題

### Q. CSSが当たっていない（スタイルが崩れている）
- **原因1: Tailwindの反映遅延**
  - CDNを使用しているため、インターネット環境が不安定だとスタイルが読み込まれないことがあります。
- **原因2: 独自クラスの打ち間違い**
  - `alert-info` などのスペルが正しいか確認してください。

### Q. 検索窓で何もヒットしない
- **原因1: インデックス未作成**
  - ビルド時に `metadata.json` が正しく生成されているか確認してください。
- **原因2: Cloudflare KVの同期漏れ**
  - 管理画面から「KV Sync」を行っているか、または環境変数が正しいか確認してください。（静的フォールバックが効いている場合は数秒遅れて表示されることがあります）

---

## 3. 管理画面の問題

### Q. 管理画面にログインできない
- **原因:** 環境変数 `ADMIN_PASSWORD` が設定されていないか、入力ミスです。Cloudflare Pagesの「Settings」→「Environment variables」を再確認してください。

---

## 初心者がハマる「ミスの要因」3選（運用編）

1. **ファイル名の重複:**
   - `content/` 内に同じ `slug` を持つファイルを作ると、一方が上書きされます。
2. **[EN] [JP] ブロックの閉じ忘れ:**
   - `[/EN]` や `[/JP]` を書き忘れると、ページ全体のレイアウトが壊れます。
3. **メタデータのハイフン忘れ:**
   - ファイル先頭の `---` は必ず上下に必要です。これがないとエラーになります。
